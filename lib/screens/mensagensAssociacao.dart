import 'package:flutter/material.dart';



class MensagensAssociacao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ola'),
      ),
      body: Center(

      ),
    );
  }
}